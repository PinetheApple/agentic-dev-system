PHASE:dispatch

Implement the task below. Respond with exactly one JSON object as your
entire response: no prose before or after it, and no markdown code fences —
just the raw JSON object (the ```json fence below is only to illustrate the
shape), shaped as:

```json
{"status": "done" | "blocked", "summary": "<what you did or why you're blocked>"}
```

## Task to implement

{task}
