PHASE:dispatch

Implement the task below. Respond with exactly one JSON object, no prose
outside it:

```json
{"status": "done" | "blocked", "summary": "<what you did or why you're blocked>"}
```

## Task to implement

{task}
